#include "main.h"

/**
 * _strcmp - check the code
 * @s1: params 1
 * @s2: params 2
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
int i, count = 0;
for (i = 0; s1[i] != '\0'; i++)
count++;


for (i = 0; i < count; i++)
{
  if(s1[i] != s2[i]) 
}

return (dest);
}
