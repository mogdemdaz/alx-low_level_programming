#include "main.h"

/**
 * puts2 - check the code
 * @s: parameter
 * Return: Always 0.
 */
void puts2(char *s)
{
int i = 0, sum = 0;
while (s[i++])
{
sum++;
}
for (i = 0; i < sum; i += 2)
_putchar(s[i]);

_putchar('\n');
}
